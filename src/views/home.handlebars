<div class="row">
    {{#each products}}
        <div class="col-md-4 mb-4">
            <div class="card">
                <div class="card-body">
                    <h6 class="card-title">Nombre del producto: {{this.title}}</h6>
                    <p class="card-text">ID del producto: {{this.id}}</p>
                    <p class="card-text">Precio del producto: {{this.price}}</p>
                    <p class="card-text">Descripción del producto: {{this.description}}</p>
                </div>
            </div>
        </div>
    {{/each}}
</div>
